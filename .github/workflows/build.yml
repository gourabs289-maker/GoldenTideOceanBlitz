name: Build APK and AAB from URL

on:
  workflow_dispatch:
    inputs:
      web_url:
        description: 'The URL of your hosted game'
        required: true
        default: 'https://gourabs289-maker.github.io/GoldenTideOceanBlitz/'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Set up Java (Required for Android Build)
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Initialize React Native Expo Wrapper
        run: |
          npx create-expo-app wrapper-app --template blank
          cd wrapper-app
          npx expo install react-native-webview

      - name: Configure Android Package Name
        run: |
          cd wrapper-app
          cat << 'EOF' > app.json
          {
            "expo": {
              "name": "Golden Tide Ocean Blitz",
              "slug": "golden-tide",
              "version": "1.0.0",
              "android": {
                "package": "com.maker.goldentide",
                "versionCode": 1
              }
            }
          }
          EOF

      - name: Inject URL into App.js
        run: |
          cd wrapper-app
          cat << 'EOF' > App.js
          import React, { useRef, useEffect } from 'react';
          import { StatusBar, BackHandler, StyleSheet, View } from 'react-native';
          import { WebView } from 'react-native-webview';

          export default function App() {
            const webViewRef = useRef(null);

            useEffect(() => {
              const onBackPress = () => true; 
              BackHandler.addEventListener('hardwareBackPress', onBackPress);
              return () => BackHandler.removeEventListener('hardwareBackPress', onBackPress);
            }, []);

            return (
              <View style={styles.container}>
                <StatusBar hidden={true} />
                <WebView
                  ref={webViewRef}
                  source={{ uri: '${{ github.event.inputs.web_url }}' }}
                  javaScriptEnabled={true}
                  domStorageEnabled={true}
                  mediaPlaybackRequiresUserAction={false}
                  bounces={false}
                  scrollEnabled={false}
                  showsHorizontalScrollIndicator={false}
                  showsVerticalScrollIndicator={false}
                  overScrollMode="never"
                  style={styles.webview}
                />
              </View>
            );
          }

          const styles = StyleSheet.create({
            container: { flex: 1, backgroundColor: '#000000' },
            webview: { flex: 1, backgroundColor: 'transparent' },
          });
          EOF

      - name: Generate Native Android Code
        run: |
          cd wrapper-app
          npx expo prebuild --platform android --clean

      - name: Build APK (Testing)
        run: |
          cd wrapper-app/android
          chmod +x gradlew
          ./gradlew assembleRelease

      - name: Build AAB (Play Console)
        run: |
          cd wrapper-app/android
          ./gradlew bundleRelease

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: GoldenTide-APK
          path: wrapper-app/android/app/build/outputs/apk/release/app-release.apk

      - name: Upload AAB Artifact
        uses: actions/upload-artifact@v4
        with:
          name: GoldenTide-AAB
          path: wrapper-app/android/app/build/outputs/bundle/release/app-release.aab
